/* eslint-disable max-len */
import express from 'express'

// import { testController } from '../../controllers/index.mjs'

const router = express.Router()

// router.post('/uploadfile', testController.uploadFile)
// router.get('/getImageList', testController.getImageList)

export default router
